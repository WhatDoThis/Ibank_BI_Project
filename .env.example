# DB 연결 (PostgreSQL). 프로젝트 루트에 .env 파일로 복사 후 값 입력
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_user
DB_PASSWORD=your_password

# Claude API (SQL 해석용, 선택)
# CLAUDE_API_KEY=sk-ant-api03-...
